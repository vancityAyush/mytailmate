import 'package:flutter/material.dart';
import 'package:mytailmate/app/theme/app_colors.dart';

final titulo =
    TextStyle(fontSize: 24, fontWeight: FontWeight.bold, color: spotlightColor);
final subTitulo = TextStyle(fontSize: 16, fontWeight: FontWeight.bold);
const textInfo = TextStyle(
    color: Color.fromRGBO(120, 116, 116, 1),
    fontFamily: 'Mulish',
    fontSize: 16,
    fontWeight: FontWeight.normal,
    height: 1.5);
final textContent = TextStyle(fontSize: 18, letterSpacing: 0.8);

const QStyle = TextStyle(
    color: Color(0xff363D42),
    fontFamily: 'Mulish',
    fontSize: 18,
    letterSpacing: 0 /*percentages not used in flutter. defaulting to zero*/,
    fontWeight: FontWeight.w600,
    height: 1.5 /*PERCENT not supported*/
    );

const AStyle = TextStyle(
    color: Color.fromRGBO(132, 136, 140, 1),
    fontFamily: 'Mulish',
    fontSize: 16,
    letterSpacing: 0 /*percentages not used in flutter. defaulting to zero*/,
    fontWeight: FontWeight.w400,
    height: 1.5 /*PERCENT not supported*/
    );

const greyOutText = TextStyle(
    color: Color.fromRGBO(150, 144, 144, 1),
    fontFamily: 'Mulish',
    fontSize: 18,
    letterSpacing: 0 /*percentages not used in flutter. defaulting to zero*/,
    fontWeight: FontWeight.w500,
    height: 1.5 /*PERCENT not supported*/
    );
const optionStyle = TextStyle(
    color: Color.fromRGBO(70, 72, 73, 1),
    fontFamily: 'Mulish',
    fontSize: 14,
    letterSpacing: 0 /*percentages not used in flutter. defaulting to zero*/,
    fontWeight: FontWeight.normal,
    height: 1.5 /*PERCENT not supported*/
    );
const buttonStyle = TextStyle(
  fontFamily: 'Mulish',
  fontSize: 20,
  color: Colors.white,
  fontWeight: FontWeight.w700,
);

const muilshBold18 = TextStyle(
  fontFamily: 'Mulish',
  fontSize: 18,
  fontWeight: FontWeight.w800,
);
const bannerStyle = TextStyle(
    fontFamily: 'Placard Condensed',
    fontSize: 24,
    letterSpacing: 0,
    fontWeight: FontWeight.w600,
    height: 1.5 /*PERCENT not supported*/
    );

const appBarText = TextStyle(
    color: Color.fromRGBO(0, 0, 0, 1),
    fontFamily: 'Mulish',
    fontSize: 24,
    letterSpacing: 0 /*percentages not used in flutter. defaulting to zero*/,
    fontWeight: FontWeight.w700,
    height: 1.5 /*PERCENT not supported*/
    );
const muilsh70016 = TextStyle(
  color: Colors.white,
  fontFamily: 'Mulish',
  fontSize: 16,
  letterSpacing: 0 /*percentages not used in flutter. defaulting to zero*/,
  fontWeight: FontWeight.w700,
);
const muilsh600 = TextStyle(
    color: Colors.black,
    fontFamily: 'Mulish',
    fontSize: 10,
    letterSpacing: 0 /*percentages not used in flutter. defaulting to zero*/,
    fontWeight: FontWeight.w600,
    height: 1.5 /*PERCENT not supported*/
    );
const muilsh500 = TextStyle(
  color: Colors.black,
  fontFamily: 'Mulish',
  fontSize: 6,
  letterSpacing: 0 /*percentages not used in flutter. defaulting to zero*/,
  fontWeight: FontWeight.w500,
);
const bottomNavStyle = TextStyle(
    color: Color.fromRGBO(70, 72, 73, 1),
    fontFamily: 'Mulish',
    fontSize: 12,
    letterSpacing: 0 /*percentages not used in flutter. defaulting to zero*/,
    fontWeight: FontWeight.w700,
    height: 1.5 /*PERCENT not supported*/
    );

const titleStyle = TextStyle(
    color: Color.fromRGBO(3, 63, 99, 1),
    fontFamily: 'Placard Condensed',
    fontSize: 30,
    fontWeight: FontWeight.w600,
    height: 1.5);

const subTitleStyle = TextStyle(
    color: Color.fromRGBO(113, 108, 108, 1),
    fontFamily: 'Mulish',
    fontSize: 15,
    fontWeight: FontWeight.normal,
    height: 1.5);

const otpTextStyle = TextStyle(
    color: Color.fromRGBO(170, 156, 156, 1),
    fontFamily: 'Roboto',
    fontSize: 18,
    letterSpacing: 0 /*percentages not used in flutter. defaulting to zero*/,
    fontWeight: FontWeight.normal,
    height: 1.5 /*PERCENT not supported*/
    );

const QnAStyle = TextStyle(
    color: Color.fromRGBO(182, 184, 185, 1),
    fontFamily: 'Mulish',
    fontSize: 20,
    /*percentages not used in flutter. defaulting to zero*/
    fontWeight: FontWeight.w700,
    height: 1.5 /*PERCENT not supported*/
    );

const bottomTextStyle = TextStyle(
  fontFamily: 'Mulish',
  fontSize: 14,
  fontWeight: FontWeight.w700,
  height: 1.5,
);

const headerTextStyle = TextStyle(
  color: Color.fromRGBO(3, 63, 99, 1),
  fontFamily: 'Placard Condensed',
  fontSize: 36,
  fontWeight: FontWeight.w600,
  height: 1.5,
);
